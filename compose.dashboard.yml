services:
  app:
    build: .
    image: flask-health-dashboard:latest
    container_name: flask-health-dashboard
    ports:
      - "8080:8080"
    environment:
      - APP_ENV=compose
    restart: unless-stopped
